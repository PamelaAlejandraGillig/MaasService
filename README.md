# README

Rails + JWT auth 

Run rails

```
$ bundle install
$ rails db:create
$ rails db:migrate
$ rails db:seed
$ redis-server
$ rails s
```
Run test
```
$ bin/rails db:migrate RAILS_ENV=test  
$ bundle exec rspec
```


